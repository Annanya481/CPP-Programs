#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    int a, b, c, x, x1, x2, quad;
    cout<<"enter values of quadratic equation: "<<"\n";
    cin>>a>>b>>c;
    quad = a*x**2 + b*x + c = 0;
    cout<<"quadratic equation is: "<<quad;
    x1 = -b + sqrt(b**2 - 4*a*c)/2*a;
    x2 = -b - sqrt(b**2 - 4*a*C)/2*a;
    cout<<"the two roots of the quadratic equation are: "<<x1<<"\t"<<x2;
    return 0;
}
